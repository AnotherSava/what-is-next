// Shared class strings for nav-style items (top nav links + footer Credits/Sign out) so they read identically and
// pick up the same `.wn-nav` hover border. Active = accent-tinted pill; inactive = muted with a hover outline.
export const NAV_ITEM_BASE = "wn-nav flex h-[22px] items-center rounded-lg px-2.5 text-[13px] whitespace-nowrap";
export const NAV_ITEM_ACTIVE = "wn-nav-on bg-[rgba(125,149,255,0.16)] font-semibold text-white";
export const NAV_ITEM_INACTIVE = "font-medium text-[var(--color-muted)]";
