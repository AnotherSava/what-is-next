// Shared styling for the owner-console action buttons (Refresh, Back up, Sync Plex) so every "run it now" button
// reads identically across the admin surface instead of each picking its own colour.
export const ACTION_BUTTON_CLASS =
  "rounded-md bg-[var(--color-accent-strong)] px-3 py-1.5 text-sm font-medium text-white hover:bg-[var(--color-accent)] disabled:opacity-50";
