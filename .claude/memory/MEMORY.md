- [What's Next build status](project_whats_next_build.md) — full self-hosted tracker; TV Time import done then removed; Plex sync (now imports watch state continuously for tracked items, per-show cursor); /recent "Recently watched" feed; durable in-app unmarks (PlexWatchSuppression); TVDB fallback; admin status dashboard; export removed; TMDB/TheTVDB attribution on /credits + footer; download-source search links on /download; IMDb (OMDb) + director ratings; Download shows season ranges; **full UI redesign to the Claude Design poster-grid reference (2026-07-16)** — PosterCard grids, grid-density slider, per-page search/chip filters, Recent timeline, restyled Settings; ratings-visibility setting removed (cards always show a single IMDb ★ chip); slug-based /shows|/movies URLs (2026-07-17); **movie details page rebuilt frameless w/ poster-hover play/download + TMDB cast (MediaItem.cast) + Plex source spec-rows — resolution/HDR/audio/subtitles on PlexPresence (2026-07-19)**; **cast + creator for shows (MediaItem.creator) via shared TopCast/CastAvatar (onError→initials fallback); per-season Plex source on PlexPresence season rows — sourceCursor-gated, applyPresence inherits (2026-07-20)**; **show details page rebuilt to the "Shows Page - Seasons" design — frameless hero (ShowHeroPoster) + two-column seasons-accordion (SeasonList) / cast rail (CastColumn); per-season media pill vs Download menu; full per-episode/season watched-date editing (setEpisodeWatchedAt/setSeasonWatchedAt + create-missing markSeasonWatched); ⋯ kebab = single item shown only when tracked, Remove/Stop tracking (removeShowFromTracking); shared PosterFavoriteHeart, CastAvatar size prop; deleted EpisodeChecklist/ShowControls/StopTrackingButton (2026-07-21)**
