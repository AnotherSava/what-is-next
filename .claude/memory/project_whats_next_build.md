---
name: project_whats_next_build
description: "What's Next tracker — full app built (phases 0-6) and live TV Time import DONE (real data in dev.db)"
metadata: 
  node_type: memory
  type: project
---

Self-hosted TV/movie tracker replacing TV Time, in `web/` (Next 16 + Prisma 7/SQLite + Tailwind 4). Spec: `claude-desktop/IMPLEMENTATION_BRIEF.md`. Built phases 0-6 on 2026-07-09: scaffold+auth, TMDB client, importer CLI, core UI, search+dashboard, refresh/admin/backup/export, Dockerfile+compose. 68 vitest tests, lint/format/build green; Docker image builds and the compose stack (migrate→app) boots and serves (verified).

Secrets in **Doppler project `whats-next`, config `dev`** (npm scripts wrap `doppler run`). TMDB_API_TOKEN is set (real v4 read token). Owner seeded as "Sava" (id `owner`, role explicitly "owner"; schema default is now "member" — a reviewed deviation).

**Live import completed 2026-07-09** against the real export (a local TV Time export bundle, kept off-repo). Results: 83/83 series + 99/99 movies have catalog rows + UserMediaState (4 `needsDetails` stubs for TMDB-unfindable titles: 3 obscure series + 1 fan movie); 1915 episode SeenEvents + 81 movie (exact); 17+8 favorites (exact); 1 list/4 items (Scavengers Reign isn't a followed series → no catalog row). 336 unmatched episodes = 287 season-0 specials (TVDB over-tracks; excluded from counts) + 49 real (genuine TVDB↔TMDB split/reorder/extra-episode differences — the brief's "handful, don't guess"). Not an importer bug. DB backed up to `web/prisma/backups/post-import-*.db`.

**Plex integration added 2026-07-09** (src/lib/plex): read-only Plex client, sync matches Plex↔catalog by external id, PlexPresence table marks which shows/seasons you have, badges + "In Plex" filter on /shows, /admin/plex review-then-add flow (pulls Plex-only titles into tracking, importing Plex watched state as source "plex"; never overwrites TV Time history). Config: PLEX_URL + PLEX_TOKEN (+ optional PLEX_LIBRARIES allowlist) in Doppler — dev token is Plex's local `.LocalAdminToken`. Live-verified: 27 shows + 23 movies + 52 seasons matched, 2 add candidates. Nightly job also refreshes presence (no auto-add).

Adversarial review of phases 0-2 caught + fixed 3 real bugs (importer tmdbId-collision abort, permanent partial-hydration data loss, User.role default) — all with regression tests. Phases 3-5 (UI/actions/refresh) were NOT adversarially reviewed yet. Optional follow-ups: series title-search fallback for the 3 unresolved series (risky, may false-match); hydrate list-only items (Scavengers Reign); `web/` git repo initialized but uncommitted.

**TVDB fallback added 2026-07-09** (`src/lib/tvdb`): TMDB stays canonical; TVDB hydrates ONLY rows TMDB can't resolve (`tmdbId` null + `tvdbId` set — the 4 import stubs: Backrooms, HP Ten Years Later, Дыши series + HP Deathly Weapons movie). New schema (migration `add_metadata_source`): `MediaItem.metadataSource` ("tmdb"|"tvdb") + `Season.tvdbId`. `TvdbClient` (v4 lazy login→bearer, 401 re-auth, episode pagination) shares transport via new `@/lib/http` + extracted `@/lib/throttle` with the TMDB client; `hydrateShow/MovieByTvdbId` reuse a now source-aware `upsertCatalogSeason`; `refreshAll` gained a TVDB pass, `refreshOne` dispatches by which id the row carries; admin "TVDB fallback" panel; `artworks.thetvdb.com` whitelisted. 101 tests + lint/tsc/build/format green. A multi-agent adversarial review fixed 6 issues (metadataSource drift/self-heal, season ordering-type pick, movie overview via `?meta=translations`, TVDB empty-string dates, TMDB/TVDB HTTP-machinery dedup). **NOT live-verified — needs `TVDB_API_KEY` (+ optional `TVDB_PIN`) in Doppler, not yet added; admin shows "Not configured".** Uncommitted; the same working tree also carries an earlier uncommitted UI change (Admin + Sign in/out moved from the footer into the top nav).

**Current state 2026-07-10.** TVDB fallback is now LIVE-VERIFIED: `TVDB_API_KEY` added to Doppler (dev), a refresh ran, and all 4 formerly-unresolved stubs (Backrooms, HP Ten Years Later, Дыши, HP Deathly Weapons) are fully hydrated from TVDB (real episodes/overviews/posters, `metadataSource=tvdb`, needsDetails=0). The TVDB-fallback commit + top-nav commit are now landed (git log: `a1ae078`, `ed329c6`).

**Import follow-ups audited + resolved 2026-07-10** (multi-agent audit vs live dev.db). Found a real gap the report under-counted: 2 of those series — HP Ten Years Later (tvdb 283555) and Дыши (466900) — each had 8 watched episodes in the TV Time source but 0 SeenEvents. Root cause: they were TMDB-unresolved at import so no catalog episodes existed to link seens to; the later TVDB refresh created the episodes but refresh NEVER writes user state, so it didn't back-fill. Back-filled all 16 episode SeenEvents via a throwaway script (`web/scripts/backfill-followups.ts`, since deleted) that reused the importer's `matchEpisodes`/`parseWatchedAt` — episode seens 1915→1931, source `tvtime-import`, original watched dates. Also added the list-only title **Scavengers Reign** (tmdb 204154 / tvdb 421287) to the "Didn't like" list (now 5 items, Scavengers at position 0). Pre-change DB snapshot: `web/prisma/backups/pre-backfill-*.db`. Remaining items are ACCEPTED, not bugs: the 15 unmatched-WATCHED episodes (Female Stand-up 11, Firefly 3, Big Bang Theory 1 special) are genuine TVDB↔TMDB per-season numbering divergences — those slots don't exist in TMDB and the TVDB fallback structurally can't help (all 3 shows are TMDB-canonical); TV Time source preserved on disk. Decided to NOT harden the importer (one-time tool). Nothing dangling: needsDetails=1 count 0; tmdbId-null is exactly the 4 TVDB rows.

**Import logic REMOVED 2026-07-10 (uncommitted).** Imports declared one-time/done, so deleted the whole apparatus: `web/src/lib/import/` (importer/mapping/gdpr/report/schemas + tests + fixtures), `web/scripts/import.ts`, the `import` npm script, the admin "Import" panel, and the `import:lastReport` setting schema/key in `settings.ts`. KEPT: `SeenEvent.source="tvtime-import"` data + its schema comment; `web/scripts/export.ts` + `export` npm script (separate feature). Findings preserved in committed doc `docs/tv-time-import.md` (provenance, counts, the 4 TVDB-resolved titles, the back-filled 16 seens, Scavengers Reign, the 15 accepted unmatched-watched with the full list, GDPR cross-check). Code recoverable from git commit `8a1fb9d`. Tests dropped 101→83 (removed 18 import tests); tsc/lint/prettier green.

**Streaming refresh-progress UI 2026-07-10 (UNCOMMITTED — current working tree).** Manual "Refresh now" now streams a determinate progress bar. New owner-gated route `web/src/app/api/admin/refresh/route.ts` emits NDJSON; `refreshAll` gained an optional `onProgress` callback + exported `RefreshProgress` type (cron path unchanged — it passes no callback); `AdminButtons.tsx` consumes the stream and renders percentage + the single active phase + the in-flight title (truncated). A single `router.refresh()` at the end replaced the old 4× `revalidatePath` (which had strobed the dev-mode indicator). Removed the now-unused `refreshNow` server action. Admin Refresh/Backup summary lines reformatted (duration in seconds not ms; multi-line; sentence-case + correct pluralization). tsc/lint/prettier/101 tests green; route reachability live-checked (403 when unauthenticated).

**Continuous Plex watched-sync + Recently-watched feed + durable unmarks 2026-07-10 (UNCOMMITTED).** Extended Plex sync so it now imports WATCH STATE for already-tracked (matched) items on every sync, not only on adopt. `scanPlex` collects "watched signals" for matched shows (via `/allLeaves`) + movies; new single-writer `applyWatched` reconciles them into the append-only SeenEvent log as source `plex` — additive-only, de-duped vs existing events of any source. Adopt-path `importPlexWatched` refactored to funnel through the same `applyWatched`/`collectShowWatchedSignals`. **Perf:** a per-show watched-episode-count cursor (`plex:watchCursor` setting, plexRatingKey→total viewedLeafCount) skips the `/allLeaves` fetch for any show whose count is unchanged since last sync → steady-state = zero episode fetches (fixes the review's HIGH finding; trade-off: an unwatch+watch netting the same total is missed until the next real change). **Feed:** user-facing `/recent` "Recently watched" page (`web/src/lib/recent.ts`) showing ALL watch history across sources (tvtime-import + plex + app), newest watch first (`orderBy watchedAt desc, createdAt desc`), each row TAGGED by source (TV Time / Plex / App pill). Nav link always shown. Initially built Plex-only (source=plex + fetch-time order) but repointed to all-sources-with-source-tags after the owner saw the empty feed and asked to "backfill existing watched data" — the dedup (applyWatched skips episodes already logged from TV Time) meant a source=plex-only feed stays empty for this user's tvtime-seeded history. `SeenEvent.createdAt` (migration `add_seen_event_created_at`) kept as the ordering tiebreaker. **Watch-next "Watch right now" section:** `/` (dashboard) gained a TOP section of behind shows that are in Plex (playable immediately) — `getDashboard` now splits `behind` into `readyInPlex` (inPlex) + `behind` (the rest); `BehindShow` carries `inPlex`; behind rows show the gold PlexBadge.

**Tracking model rewrite — `tracking` enum → `wantToWatch` boolean 2026-07-10 (UNCOMMITTED).** The 4-value `UserMediaState.tracking` (planned/watching/stopped/finished) conflated user INTENT with watch PROGRESS (already derived from the SeenEvent log), so it drifted — a "planned" show you'd started watching in Plex stayed hidden from Behind (7 real shows affected, incl. Mr. Robot). Replaced with a single `wantToWatch Boolean @default(false)` — the ONLY stored intent; every display bucket is DERIVED via rewritten `progress.ts displayGroup(wantToWatch, progress)`: finished (all aired watched+ended) → Finished regardless of flag; 0 watched → Planned if wanted else "off-list" (new hidden default, filtered out of getFollowedShows/getMovies); started+wanted → Behind/Up-to-date; started+not-wanted → Stopped. New `VisibleGroup = Exclude<DisplayGroup,"off-list">`. Migration `20260710150000_replace_tracking_with_want_to_watch` (HAND-AUTHORED — prisma migrate dev refused non-interactively on the destructive drop; applied via `migrate deploy`) maps stopped→false, else→true (169 true / 15 false). Accepted consequence: "How to Get Away with Murder" (stopped, 0 watched) is now off-list/hidden. UI: 4-option `TrackingSelect` dropdown → `WantToWatchToggle` (On my list / Resume / Add to list); `setTracking`→`setWantToWatch`. Updated shows.ts, movies.ts, dashboard.ts, shows/movies/search actions, plex adopt path, ShowControls, show detail page, 3 test files. Adversarial 4-dimension review (12 agents) found 6 confirmed → ALL FIXED: (1) displayGroup ordering bug — a 0-aired Ended/Canceled show with 0 watched was bucketed "finished" instead of Planned/off-list; moved the nothing-watched gate ahead of the finished check (+regression test); (2) WantToWatchToggle mislabeled finished shows ("marks it Stopped"/"Resume" no-ops) — hidden for finished shows; (3) off-list hid favorited shows — favorited off-list now shows as Planned (never hidden); (4) card/detail summary read raw progress.status (a Planned show said "N to watch" in behind-amber) — new `groupSummary(group, progress)` helper keys on the display group; added `group` to ShowDetail; (5) stale "Import your TV Time export" home empty-state copy removed. 92 tests + tsc/eslint/prettier/build green. Still UNCOMMITTED.

**Admin Settings section + "Enable manual watched toggle" flag 2026-07-10 (UNCOMMITTED).** New `settings:manualWatched` Setting (`{enabled:boolean}`, `isManualWatchedEnabled()` helper defaults FALSE when absent — no seed). `/admin` gained a "Settings" section with a checkbox (`ManualWatchedToggle` in AdminButtons.tsx → `setManualWatched` server action, revalidates /admin,/,/shows,/movies). When OFF (default), ALL manual mark-watched CONTROLS are hidden while read-only watched STATE display stays: home green "Watched" button (page.tsx, `canMarkWatched`), EpisodeChecklist toggles/mark-season/up-to-here (shows/[id] passes `canEdit && manualWatched` — component already renders static ✓ when canEdit=false), movie Mark/Unmark (movies/page.tsx `canMarkWatched`; favorite star stays on canEdit). Rationale: watch state comes from Plex sync. 7 files, no migration. Green. **Durable unmarks:** new `PlexWatchSuppression` table (migration `add_plex_watch_suppression`) — unmarking a watch in-app (episode or movie) records a suppression so the sync won't re-import it; re-marking watched clears it; `applyWatched` skips suppressed signals. Wired into all watch-mutation sites in `shows/actions.ts` + `movies/actions.ts`. Extracted shared `plural()` → `web/src/lib/format.ts`; admin summaries pluralized + show "watches imported". Adversarial 4-dimension review (14 agents) ran; 6 confirmed findings — 4 fixed (perf cursor + 3 UI-consistency), 1 left by user choice→built (durable unmarks), 1 left as-is (pre-existing 5000-item Plex container cap — pagination offered, declined for now). 88 vitest tests + tsc/eslint/prettier/`next build` green. Generated Prisma client stays gitignored (regen via `prisma generate`).

**"Open Plex link to watch" 2026-07-10 (UNCOMMITTED).** Every in-Plex show/movie now deep-links to its page in the Plex web app to play it. New nullable `PlexPresence.plexRatingKey` (migration `add_plex_rating_key`) stores each present item's Plex ratingKey; `scanPlex`/`applyPresence` capture `item.ratingKey` per presence row (`PresenceRow.plexRatingKey` now required). The server `machineIdentifier` is fetched via new `PlexClient.getMachineIdentifier()` (GET `/identity`, `plexIdentitySchema`) and stored best-effort each sync as Setting `plex:server` (`getPlexServerId()`); a `try/catch` in `run.ts` keeps a `/identity` failure from aborting the sync. `src/lib/plex/link.ts` builds the link — `plexWebUrl(machineId, ratingKey)` → `https://app.plex.tv/desktop/#!/server/{machineId}/details?key=%2Flibrary%2Fmetadata%2F{ratingKey}` (both parts `encodeURIComponent`'d); `plexWatchUrl(machineId|null, ratingKey|null|undefined)` returns null unless both present. Item-level (not per-episode) on purpose: Plex's own Play/Resume button on the details page honors the saved offset, so it resumes a partly-watched item — and it keeps the sync's zero-episode-fetch cursor. Read helpers: `getShowsInPlex`→**renamed** `getPlexPresenceKeys(userId): Map<mediaItemId, ratingKey|null>` (serves shows list + movies list); `getShowPlexPresence` also returns `ratingKey`. Data layers carry `plexRatingKey`: `ShowSummary`/`ShowDetail` (shows.ts), `MovieSummary` (movies.ts — **movies got Plex presence for the first time**: `inPlex`+`plexRatingKey` via `getPlexPresenceKeys`), `BehindShow` (dashboard.ts). UI: `PlexBadge` with `href` renders a **small circular play button** (20px `rounded-full` chip; neutral at rest — `bg-[var(--color-surface-2)]` + `text-[var(--color-muted)]` triangle — revealing Plex gold only on hover: `hover:bg-[#e5a00d]/15 hover:text-[#e5a00d]`; inline-SVG **equilateral** play triangle (vertices `(19,12)`,`(8.5,18.06)`,`(8.5,5.94)` — all radius 7 from viewBox centre 12,12, so optically centred with no nudge), title/aria "Watch in Plex", opens new tab); `dot` variant (season presence dots) unchanged. Wired into all 4 surfaces (home "Watch right now"/"Behind" rows, /shows list, /shows/[id], /movies) with identical `{watchUrl && <PlexBadge href={watchUrl} />}` gating — a play icon means playable, so gated on the URL not mere presence (`inPlex` still used for the /shows "In Plex" filter + dashboard readyInPlex split). Started as a gold "▶ Plex" pill; owner asked for a less-fancy play icon instead of "yellow pill everywhere" → switched. Link is NOT gated on canEdit (shows to anonymous showcase viewers like the badge always did; machineIdentifier isn't secret — `/identity` is unprotected). Tests: link.test.ts (5) + 2 presence-ratingKey assertions in sync.test.ts → **95 pass**; tsc/eslint/prettier/`next build` green. Live-verified: ran a real sync — all 81 presence rows back-filled with ratingKey, `plex:server`=`a redacted machine identifier`, sample link built correctly. Adversarial 4-dimension review (6 agents) → 0 confirmed defects; 1 low nit (home badge gating) fixed, 1 (presence.ts untested) refuted/accepted. NOTE: regenerating the Prisma client under a *running* `next dev` leaves it with a stale in-memory client (500s "Unknown field plexRatingKey" on Plex pages) until restart — not a code bug.

**Admin dashboard + export removal + attribution 2026-07-10.** (Streaming-refresh + import-removal now COMMITTED: `41e34bd`, `576c3ef`.) `/admin` rewritten from stacked panels into an at-a-glance STATUS DASHBOARD — one card per scheduled job (Refresh, Plex sync, Backup) with a coloured freshness dot + "up to date · Nh ago"/"stale"/"failed" badge (26h staleness window = daily cron + grace); TVDB-stub completeness folded into the Refresh card (no standalone TVDB card); Plex last-sync surfaced on `/admin` with a "review & add" amber callout for untracked candidates; Plex sync action now also revalidates `/admin`. Shared `ACTION_BUTTON_CLASS` (`web/src/app/admin/_components/buttonStyle.ts`) unifies all run-now buttons (Plex sync dropped its `#e5a00d` gold). EXPORT feature REMOVED (`web/scripts/export.ts` + `export` npm script gone; `web/scripts/` now empty/removed — use Prisma Studio or `sqlite3 .dump` on a backup if a dump is ever needed). ATTRIBUTION restructured for TMDB/TheTVDB compliance: TMDB notice + official logo → new `/credits` page (their About/Credits requirement), reachable from a footer link; TheTVDB official logo (white-for-dark variant) + "Sign out" moved into the footer; footer is now a sticky bottom bar (`body flex min-h-dvh flex-col` + `main flex-1`); logos in `web/public/{tmdb.svg,thetvdb.png}`. Both memos done (`.claude/memos.md`). See global learning `tmdb-thetvdb-attribution.md`. Uncommitted at time of writing.

**Plex sync-duration timing + on-view freshness (SWR) + header freshness dot + Watch-next 3-line rows 2026-07-11 (UNCOMMITTED at reflection; about to /commit).** (1) *Timing:* `syncPlexPresence` times itself (`durationMs` → `plexLastSyncSchema` `.default(0)`, returned, logged in the nightly line, shown on `/admin` + `/admin/plex`); `seconds()` formatter promoted to `format.ts`. (2) *On-view freshener* (chose stale-while-revalidate over any scheduled/every-minute polling): `PlexFreshener` client component in the root layout (owner+Plex only) fires `freshenPlexOnView()` on mount, tab-focus, and a 20s poll (all `visibilityState==="visible"`-gated). `syncPlexPresenceIfStale` throttles to one `PLEX_VIEW_TTL_SECONDS` window (default 60s) + coalesces via a module-level in-flight promise; client `router.refresh()`es on `synced` (not just changed) so React reconciliation does "update-in-place-if-different" and a no-change sync still resets the dot. `syncPlexPresence` gained `"view"` trigger + `presenceChanged`; `applyPresence` returns whether the set changed (before/after signature sets). Render never blocked (paints from DB, freshen after hydration). (3) *Freshness dot* (`FreshnessDot`, left of Admin nav link, owner+Plex only): green ≤1.5min / yellow / red >3×window (user's "3× interval" rule → 3min at the 60s window); self-ticks 15s so it ages on an idle open page; holds optimistic-green ~3s on load so it never flashes stale→green (a freshen always fires on load; reveals stale colour only if the sync can't refresh). New `--color-bad:#e5484d` token (palette had no red); layout passes `{lastSyncAt, staleThresholdMs:3*viewSyncTtlMs()}`. (4) *Watch-next BehindRow* ("Watch right now"/"Behind" cards): `S01E05`→`Season 1, Episode 5` (helper `code`→`episodeLabel`, dropped mono), episode name on its own 3rd line (`min-h-5` holds the line when a title is absent), right meta column mirrors 3 lines via `justify-between self-stretch` (last-watched top, "+N more" bottom, empty middle); UpcomingRow label updated too. *Design evolution this session:* nightly-cron-only → on-view SWR (rejected server cron / every-minute polling); briefly split into two windows (5min general / 1min open) then **collapsed to one 60s window** after the owner wanted "sync as soon as I open it" (the 5min-on-mount caused a ~30s-to-first-poll delay); poll set to 20s (<window) so data never ages past the 1.5min green line (no green↔yellow flicker). tsc/eslint/vitest (20 plex tests, +1 for the applyPresence change-signal) green each step; verified via server logs (mount real-sync ~230ms, throttled polls ~13ms) — couldn't screenshot (Next dev HMR blocks claude-in-chrome idle; `javascript_tool` DOM reads would've worked, per learning `claude-in-chrome-dev-server.md`).

**"Download" view + per-episode Plex presence 2026-07-12 (UNCOMMITTED).** New owner-only `/download` page (nav link right after Watch next; page renders for displayedUser, reachable read-only by URL like the rest) listing tracked shows with aired episodes NOT in your Plex library, split like Watch-next: **In progress** (Behind-group shows, ordered by most-recent watch) + **Not started** (Planned-group shows, ordered by missing count). The key enabler: PlexPresence is per-SEASON, which can't see the main download case — a season you already partly own that's missing newer aired episodes. So added **per-episode** presence: new `PlexEpisodePresence` table (migration `20260711160000_add_plex_episode_presence`; `{userId, mediaItemId, episodeId}`, unique `[userId, episodeId]`). `scanPlex` now also tracks a **presence cursor** (`plex:presenceCursor` setting = plexRatingKey→total `leafCount`, mirroring the watch cursor) and fetches `/allLeaves` when EITHER the watched-count OR the total-episode-count moved — that one fetch feeds both watched signals and the episodes-present snapshot (steady-state still = zero episode fetches). New `applyEpisodePresence` is INCREMENTAL (not a full snapshot like `applyPresence`): replaces rows only for shows re-fetched this sync, drops rows for shows no longer matched (`notIn matchedShowIds`), keeps untouched shows' rows. `ScanResult` gained `presenceCursor`/`matchedShowIds`/`episodePresence`; refactored watched-signal extraction into pure `watchedSignalsFromEpisodes`/`episodePresenceFromEpisodes`. Read: `getPlexEpisodePresence(userId): Set<episodeId>`. New `web/src/lib/download.ts` — reuses `getFollowedShows` for grouping (no drift) + a pure, unit-tested `missingFromPlex(episodes, watchedIds, presentIds, today)` (aired ∧ unwatched ∧ not-in-Plex ∧ non-special, sorted). Extracted shared `web/src/app/_components/Section.tsx` (home page.tsx now imports it). Whole view is Plex-gated (empty state when `!isPlexConfigured`). **Home "Watch right now" now uses the same per-episode presence** (2026-07-12 follow-up): `getDashboard` puts a behind show in "Watch right now" only when its NEXT-UP episode is in Plex (was: show-level `inPlex`), and the home play button is gated the same way — `BehindShow.inPlex`→`nextUpInPlex`. So a show you have in Plex but whose next unwatched episode isn't downloaded (e.g. Black Mirror next-up S2E4, Female Stand-up S5E1) is excluded from "Watch right now" and appears in Download instead. **Follow-ups (2026-07-12):** the Download "In progress" section was split into **"Get back"** (started, 0 unwatched aired episodes left in Plex — you're blocked) and **"More of"** (started, still have unwatched aired episodes in Plex), both ordered by last-watched date desc; the split predicate + comparators were extracted to a pure, unit-tested `classifyDownloads()` (alongside pure `unwatchedInPlexCount()`), and the dead `DownloadEpisode.episodeId` field was dropped. The home **"Behind" section was then removed entirely** — Watch next now shows only "Watch right now"; `Dashboard` is now just `{readyInPlex}` (behind-but-next-episode-not-in-Plex shows live solely in the Download view). **Live-verified**: a real sync wrote 504 episode-presence rows across 28 shows; `/download` renders HTTP 200 with Black Mirror **S2E4** surfaced (aired/unwatched/not-in-Plex despite other S2 episodes being present — the exact within-season gap season-level presence would miss). 116 vitest (+5 download, +5 sync episode-presence) + tsc/eslint/prettier green. NOTE (again): regenerating the Prisma client under a *running* `next dev` leaves a stale in-memory client → `undefined.findMany` 500s on the new table until restart; restart via `bash scripts/deploy.sh`.

**Tracking ✕ + heart favorites + movie detail page + Watch-next trim 2026-07-11 (UNCOMMITTED at reflection; about to /commit; the ✕ finishes the "replace On-my-list with an X that confirms" memo).** (a) *Tracking ✕*: the show "✓ On my list" button → a shared `web/src/app/_components/StopTrackingButton.tsx` — an ✕ that expands to an inline "Stop tracking? ✓ ✕" (no native `confirm()`); `onConfirm` prop bound by the caller. `WantToWatchToggle`→`TrackToggle` (now takes `isFavorite`), `setWantToWatch`→`setTracking`; untracked labels "Add to list"→"Track", "Resume" kept. (b) *Movies-vs-shows tracking model divergence* (the key non-obvious call): investigated the data — **movies never have `wantToWatch=false`** (0 rows; every movie write path — add/mark/unmark/favorite — sets it true), so the flag is vestigial for movies and `movies.ts`'s `if (!isWatched && !st.wantToWatch) continue` guard was dead. So movie tracking = **row presence**: new `untrackMovie` action DELETES the `UserMediaState` row (exact inverse of add, re-addable from Search; removed the dead guard); shows keep the flag with off-list/Stopped. The ✕ shows on movie WATCHLIST cards only, moved to the **top-right** of the title row. (c) *Favorites = hearts*: ★/☆→♥/♡ at all sites; the ♡ **toggle now lives only on the detail pages** — lists (`/shows`, `/movies`) show a read-only ♥ badge (dropped `FavoriteStar`/`MovieFavoriteStar` + the now-dead `canEdit` plumbing from `/shows`). A **favorited show can't be untracked** (`TrackToggle` hides the ✕ when `isFavorite` — favorites protect visibility, so untracking wouldn't hide it anyway). Movie favorite only for **watched** movies (unwatched can't be favorite — 0 in DB, so no cleanup). (d) *New movie detail page* `web/src/app/movies/[id]/page.tsx` (mirrors the show page: poster/title-year/status/overview + favorite toggle for watched + mark/unmark), backed by new `getMovieDetail` in `movies.ts`; movie list titles now Link to it; `revalidateMovies(movieId?)` also revalidates `/movies/[id]` so its controls refresh. (e) *Watch-next trim*: removed the "Airing in the next 2 weeks" + "Movie watchlist" sections from `/` (page.tsx) and their now-dead computation from `dashboard.ts` (`upcoming` query, `watchlistMovies` fetch, `UpcomingEpisode` type, `UPCOMING_WINDOW_DAYS`, `getMovies`/`isoDatePlusDays` imports) — `Dashboard` is now `{readyInPlex, behind}`. 106 vitest + tsc/eslint/prettier green. Owner-only controls (the ✕, the toggles) NOT driven in-browser — the claude-in-chrome tab is anonymous on the dev port (no owner session; login is a password entry I can't do) and the extension later disconnected; read-only states + detail-page renders verified via curl (see the learning's RSC double-count caveat). Prior commits this session: `6aa8d21`/`bb56d00`/`59abe98` (specials-last + next-up-only expand, month/year interval units, Plex date-reconciliation doc).

**Two-column Watch-next/Download + unaccounted Plex items + /admin/plex merged into /admin 2026-07-12 (UNCOMMITTED; about to /commit).** (a) *Two columns:* `/` (Watch next) and `/download` now render Movies (left) + Shows (right) via `md:grid-cols-2` (stack Movies-first on mobile). Watch-next Movies = unwatched watchlist movies IN Plex (playable); Download Movies = watchlist movies NOT in Plex AND released (`hasAired` filter, mirroring the aired-episode rule). `getDashboard` returns `readyMovies`; `getDownloads` returns `movies` (shows keep Get-back/More-of/Not-started, stacked in the right column). New `MovieSummary.inPlex` (presence-map membership — the precise "available in Plex" signal); shared `web/src/app/_components/EmptyColumn.tsx` placeholder; `classifyDownloads` return type split into `ShowDownloads` so it stays movie-agnostic (its unit tests unchanged). (b) *Unaccounted Plex items:* `scanPlex` no longer silently drops Plex library items that match no catalog entry AND have no external id (Plex "local"/unmatched files) — they're collected as `ScanResult.unaccounted` (`UnaccountedItem`), persisted to Setting `plex:unaccounted` + an `unaccounted` count on `plex:lastSync`, and listed on `/admin`. Motivating case: **"The Salt of the Earth"** (Plex ratingKey 312, guid `local://312`, no TMDB/IMDb/TVDB id, junk title, null year) was invisible — neither matched (→ Watch next) nor an add-candidate → sat only in Download's Movies column despite being in Plex. Remedy for such items is Plex-side (Fix Match); the view just surfaces them (id-only matching is deliberate — never fuzzy title). (c) */admin/plex REMOVED, merged into /admin:* deleted the route (`page.tsx`/`actions.ts`/`_components/SyncButton.tsx`); `SyncPlexButton`→`admin/_components/SyncButton.tsx`, `syncPlexNow`+`addSelectedPlexItems`→`admin/actions.ts`. The "In Plex but not tracked" review-and-add form + the new "Unmatched in Plex" list both live in the /admin Plex-sync card now, each hidden when its count is 0 (no empty-state text); the card's dot goes amber when either has items. 123 vitest + tsc/eslint/prettier green; live-verified via minted owner-session curl (`unaccounted`=1 = The Salt of the Earth; /admin renders both, /admin/plex 404s).
